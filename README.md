# Scripts
Public repository containing scripts I use in my daily routine:

### iterm_ssh_profile.sh
Auxiliary script for iTerm2 that automates VPN and SSH connectivity. It establishes a VPN connection before initiating the SSH session and disconnects it automatically when the session ends.
